import { LoaderCircle } from 'lucide-react'

export function Card({ children, className = '' }) { return <section className={`rounded-xl border border-slate-200 bg-white shadow-soft ${className}`}>{children}</section> }
export function MetricCard({ label, value, note, icon: Icon }) { return <Card className="p-5"><div className="flex items-start justify-between"><div><p className="text-xs font-bold uppercase tracking-[.14em] text-slate-500">{label}</p><p className="mt-3 text-3xl font-extrabold tracking-tight text-slate-950">{value}</p><p className="mt-1 text-sm text-slate-500">{note}</p></div>{Icon && <span className="rounded-lg bg-indigo-50 p-2.5 text-indigo-600"><Icon size={18} /></span>}</div></Card> }
export function Badge({ children, tone = 'indigo' }) { const tones = { indigo: 'bg-indigo-50 text-indigo-700', green: 'bg-emerald-50 text-emerald-700', amber: 'bg-amber-50 text-amber-700' }; return <span className={`inline-flex rounded-full px-2.5 py-1 text-xs font-bold ${tones[tone] || tones.indigo}`}>{children}</span> }
export function Button({ children, variant = 'primary', className = '', ...props }) { return <button className={`inline-flex items-center justify-center gap-2 rounded-lg px-4 py-2.5 text-sm font-bold transition focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2 ${variant === 'primary' ? 'bg-indigo-600 text-white hover:bg-indigo-700' : 'border border-slate-200 bg-white text-slate-700 hover:border-indigo-200 hover:text-indigo-700'} ${className}`} {...props}>{children}</button> }
export function ProgressBar({ value, color = 'bg-indigo-500' }) { return <div className="h-2 w-full overflow-hidden rounded-full bg-slate-100"><div className={`h-full rounded-full ${color}`} style={{ width: `${value}%` }} /></div> }
export function LoadingState() { return <div className="flex items-center gap-3 p-8 text-sm text-slate-500"><LoaderCircle className="animate-spin text-indigo-600" size={20} /> Loading workspace data...</div> }
export function EmptyState({ title = 'No prediction yet', description = 'Generate a profile prediction to unlock this view.' }) { return <div className="rounded-xl border border-dashed border-slate-300 bg-slate-50 p-8 text-center"><p className="font-bold text-slate-800">{title}</p><p className="mt-1 text-sm text-slate-500">{description}</p></div> }
export function ChartCard({ title, description, children }) { return <Card className="p-5"><div className="mb-5"><h3 className="font-bold text-slate-900">{title}</h3>{description && <p className="mt-1 text-sm text-slate-500">{description}</p>}</div>{children}</Card> }
export function ScoreGauge({ value }) { const radius = 54; const circumference = 2 * Math.PI * radius; const progress = Math.min(100, Math.max(0, Number(value) || 0)); const offset = circumference - (progress / 100) * circumference; return <div className="relative h-36 w-36 shrink-0" aria-label={`Predicted Exam Score ${Number(value).toFixed(2)}`}><svg viewBox="0 0 128 128" className="h-full w-full -rotate-90"><circle cx="64" cy="64" r={radius} fill="none" stroke="#e0e7ff" strokeWidth="10" /><circle cx="64" cy="64" r={radius} fill="none" stroke="#6366f1" strokeWidth="10" strokeLinecap="round" strokeDasharray={circumference} strokeDashoffset={offset} /></svg><span className="absolute inset-0 flex items-center justify-center text-2xl font-extrabold text-slate-950">{Number(value).toFixed(2)}</span></div> }
export function RecommendationCard({ recommendation }) { const tone = recommendation.priority === 'high' ? 'amber' : recommendation.priority === 'medium' ? 'indigo' : 'green'; return <Card className="p-4 transition hover:-translate-y-0.5 hover:shadow-soft"><div className="flex items-start gap-3"><div className={`mt-1 h-2 w-2 shrink-0 rounded-full ${recommendation.priority === 'high' ? 'bg-amber-500' : recommendation.priority === 'medium' ? 'bg-indigo-500' : 'bg-emerald-500'}`} /><div><div className="flex flex-wrap items-center gap-2"><h3 className="font-bold text-slate-900">{recommendation.category}</h3><Badge tone={tone}>{recommendation.priority}</Badge></div><p className="mt-2 text-sm leading-6 text-slate-600">{recommendation.message}</p><p className="mt-2 text-xs leading-5 text-slate-400">{recommendation.reason}</p></div></div></Card> }
