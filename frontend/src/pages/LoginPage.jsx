import { useState } from 'react'
import { Link, useLocation, useNavigate } from 'react-router-dom'
import { ArrowRight, BrainCircuit } from 'lucide-react'
import { Badge, Button, Card } from '../components/ui'
import { useAuth } from '../auth/AuthContext'

export default function LoginPage() {
  const { login } = useAuth(); const navigate = useNavigate(); const location = useLocation(); const [form, setForm] = useState({ email: '', password: '' }); const [error, setError] = useState(''); const returnTo = new URLSearchParams(location.search).get('returnTo') || '/dashboard'
  const submit = async event => { event.preventDefault(); setError(''); try { await login(form); navigate(returnTo, { replace: true }) } catch (loginError) { setError(loginError.message) } }
  return <div className="flex min-h-screen items-center justify-center bg-slate-50 px-5 py-10"><Card className="w-full max-w-md p-8"><Link to="/" className="flex items-center gap-3"><span className="rounded-xl bg-indigo-600 p-2 text-white"><BrainCircuit size={20} /></span><span className="font-extrabold">Student Performance AI</span></Link><Badge>Student workspace</Badge><h1 className="mt-5 text-3xl font-extrabold">Welcome Back</h1><p className="mt-2 text-sm leading-6 text-slate-500">Log in to access your profile and prediction workspace.</p>{error && <div role="alert" className="mt-5 rounded-lg border border-red-200 bg-red-50 p-3 text-sm font-bold text-red-700">{error}</div>}<form onSubmit={submit} className="mt-7 space-y-5"><label className="block"><span className="mb-2 block text-sm font-bold">Email</span><input className="field" type="email" autoComplete="email" required value={form.email} onChange={event => setForm({ ...form, email: event.target.value })} /></label><label className="block"><span className="mb-2 block text-sm font-bold">Password</span><input className="field" type="password" autoComplete="current-password" required value={form.password} onChange={event => setForm({ ...form, password: event.target.value })} /></label><Button className="w-full" type="submit">Log In <ArrowRight size={16} /></Button></form><p className="mt-6 text-center text-sm text-slate-500">Forgot password? This local prototype has no password reset flow.</p><p className="mt-2 text-center text-sm text-slate-500">Don't have an account? <Link className="font-bold text-indigo-600" to="/signup">Create Account</Link></p></Card></div>
}
