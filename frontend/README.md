# Student Performance AI Frontend

React/Vite frontend foundation for the Student Performance Prediction System.

## Run locally

```powershell
npm install
npm run dev
```

The API boundary is centralized in `src/services/api.js` and uses `VITE_API_URL`. Prediction form submission remains intentionally disconnected until the next integration phase.
